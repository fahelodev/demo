apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-demo
  template:
    metadata:
      labels:
        app: service-demo
    spec:
      containers:
        - name: microservices
          image: microservices:latest
          imagePullPolicy: Never
